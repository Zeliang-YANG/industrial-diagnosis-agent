# 安全说明

## 敏感信息

请勿在 Issue、Pull Request、日志或代码中提交 DeepSeek API Key、数据库密码、Bearer Token、设备凭据或真实工业数据。项目只跟踪 `.env.example`，本机 `.env` 已被 `.gitignore` 排除。

如果凭据被意外提交，请立即在对应服务中撤销并重新生成凭据，然后清理 Git 历史；仅删除最新提交中的字符串并不能消除泄露。

## 运行边界

本项目用于本地仿真和学习演示。Agent Tools 设计为只读，不应被扩展为直接控制 PLC、绕过安全联锁或执行未经审核的维修动作。连接真实工业环境前，需要完成网络隔离、最小权限、数据库只读账号、设备点位白名单和现场安全评审。

## 报告问题

请通过 GitHub 仓库的 Security 页面私下报告安全问题，避免在公开 Issue 中披露可利用细节或敏感数据。
