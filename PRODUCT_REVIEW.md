# 工业设备智能诊断平台：产品检查

检查日期：2026-09-07

## 产品定位

当前最合适的定位是“面向产线负责人和设备工程师的工业数据诊断 Agent 演示产品”。核心任务不是展示更多图表，而是让用户从一个异常问题出发，在较短时间内获得可追溯的指标分解、事件证据、数据限制和排查方向。

建议用这一句话介绍项目：

> 用户用自然语言询问设备效率异常，Agent 自动查询 MySQL 中的 KPI、状态与故障事件，并检索独立的工业诊断知识库，输出带事件 ID 和知识库引用的诊断结论。

## 当前可演示的三条核心路径

1. 设备诊断：选择工位，询问某设备某日 OEE 与故障原因，查看 Tool Calling 轨迹和事件证据。
2. 知识问答：直接询问 OEE、CNC、机器人、PLC/OPC UA 或数据质量问题，Agent 调用诊断知识库并引用文档 ID。
3. 可信拒答：缺少设备、日期或有效数据时，Agent 请求补充信息或明确说明证据不足。

## 本次已经修正

- 浏览器和页面标题由开发模板改为产品名称。
- 首屏明确标注“模拟数据”、DeepSeek、4 个 Tools 和诊断知识库 RAG。
- 连接状态会根据接口结果显示“连接中、已连接、连接异常”。
- 页面时间统一为中文 24 小时制，并显示数据日期与刷新时间。
- 首次进入即可试用通用诊断知识问题，不再出现“主体已有数据但 Agent 仍要求先选择”的冲突。
- Agent 回答支持经过清洗的 Markdown，包括标题、列表和表格；长回答自动滚动到底部。
- KPI 阈值明确标为演示阈值，避免被误解为行业标准。
- 规则生成的分析和维护建议明确标记为模拟、待现场验证。
- 设备级数据警告会转成用户可理解的数据质量说明。
- 窄屏下 Agent、功能树和指标内容改为纵向布局。

## 当前成熟度判断

作为求职作品和本机面试演示，核心闭环已经成立。它已经能说明实时采集、数据建模、工具调用、RAG、证据约束和 Agent 评估，不再只是给原看板增加一个聊天框。

当前仍属于单机演示产品。直接部署给真实工厂使用前，还缺少账号权限、审计、配置管理、厂商知识接入、生产数据治理和系统监控。

## 后续优先级

### P0：面试演示前完成

1. 固定一套 3–5 分钟演示脚本，覆盖设备诊断、知识库问答和证据不足拒答。
2. 建立稳定的数据快照。当前仿真持续变化，同一问题每次结果不同，不利于面试复现。
3. 扩充 Agent 评估集到至少 15 个案例，加入工具参数正确率、数值忠实度、引用正确率和限制披露。
4. 为每次回答生成诊断编号，并支持复制“结论 + 证据 + 限制”，方便展示完整交付结果。

### P1：形成完整 MVP

1. 增加设备手册、报警码表和维护记录的导入流程，保存文档版本、设备型号和页码来源。
2. 将阈值、理想节拍、设备权重做成可配置项，并记录配置版本；历史 KPI 必须按当时配置解释。
3. 增加会话历史，让用户可以继续追问同一设备和时间窗口，同时控制上下文长度。
4. 在产线和车间层聚合数据质量，避免平均值掩盖单台设备的采集空档或事件异常。
5. 增加模型调用日志、延迟、token 成本、工具错误率和请求 ID，便于排查失败。

### P2：考虑真实部署时完成

1. 身份认证、角色权限、数据库只读账号、接口限流和审计日志。
2. 将前端 API 地址、模型和阈值移到环境配置，收紧生产环境 CORS。
3. 增加 OPC UA 断线重连、设备时钟校准、数据补传与告警机制。
4. 对前端做按需加载。当前生产包约 722 KB gzip，适合本机演示，但正式部署可继续拆分 ECharts、Element Plus 和调试插件。

## 面试时应主动说明的边界

- 当前连接的是 Python OPC UA 仿真设备，数据写入独立 MySQL 演示库。
- 故障码和排查项来自项目模拟规则，不是厂商认证结论。
- RAG 使用项目独立维护的小型工业诊断知识库和可解释检索，没有伪装成厂商知识库或大规模企业知识库。
- Agent 只具备查询能力，不会执行任意 SQL，也不会控制或修改设备。
- 诊断输出是决策支持，物理根因仍需现场数据或厂商材料确认。
