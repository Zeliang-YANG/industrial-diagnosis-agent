# 工业设备诊断 Agent 工程审查

审查日期：2026-09-09
审查范围：Agent 编排、Tools、RAG、评测、可观测性、安全、数据工程、交付与前端。

## 结论

当前项目达到 **7.8/10：可试运行的企业级原型，强 Agent 实习项目**。它已经不是“聊天框接大模型”，而是一套能从 MySQL 和工业知识库取证、约束工具调用、校验证据并披露数据边界的诊断 Agent。

和上一版 6.5/10 相比，主要提升来自 3 个工业证据工具、确定性高置信路由、运行时可靠性与审计、会话上下文、数据索引、系统化评测、CI 和前端拆包。距离真正生产级系统仍约 **2.2 分**，差距集中在组织身份与权限、多实例基础设施、厂商知识资产、生产可观测平台和更大规模评测，而不是再增加几个演示工具。

## 能力评分

| 维度 | 当前 | 证据与边界 |
| --- | ---: | --- |
| Agent 编排 | 8.5/10 | 支持相关工具收敛、确定性预执行、模型工具调用循环、轮次/调用上限、短期上下文、无证据降级。会话仍是单实例内存状态。 |
| Tool 设计 | 8.5/10 | 7 个严格 schema 的只读工具，覆盖设备、遥测、产线和知识；设备/产线白名单、分页、时间窗和结果上限完整。 |
| Grounding | 8.0/10 | 校验 `KB-*`、`status_event_log:*`、`raw_telemetry:*` 是否来自本次实际结果；未支持引用会降为 `needs_review`。尚未对所有自然语言数值逐字段核验。 |
| RAG | 7.5/10 | 6 份独立手册、27 块、来源属性、复核日期、内容哈希、revision、文件变更自动重载；14 条基准 Hit@3=1.0、MRR=0.9615。资料仍是项目自建，规模小。 |
| Evaluation | 7.5/10 | 44 项离线/集成测试，8 条真实模型案例，检查精确参数、引用、越权、token 与分页边界。生产前还需 30–50+ 固定 fixture 与字段级事实评测。 |
| 可观测性 | 7.5/10 | request ID、模型/provider 请求、尝试次数、模型/工具/总耗时、token、grounding 和轮转 JSONL 审计日志。未接 OpenTelemetry、指标平台和告警。 |
| 安全与权限 | 6.5/10 | 密钥只在后端、策略预拦截、固定只读工具、32 KB 请求限制、CORS 白名单、可选 Bearer token、审计日志不保存正文。缺少 IdP/JWT、RBAC、租户隔离和密钥管理服务。 |
| 运行可靠性 | 7.0/10 | 超时、可恢复错误重试/退避/抖动、并发信号量、异常隔离、健康检查和优雅退出。仍是同步 Flask 单进程，没有队列、熔断、模型降级或多实例状态。 |
| 数据工程 | 7.5/10 | 跨午夜、空档、重叠事件和计数器重置均有口径；新增遥测/事件复合索引与日报唯一索引。缺 Alembic 和独立只读运行账号。 |
| 交付与前端 | 6.5/10 | Git、`.gitignore`、MySQL CI、离线检索评测、生产构建、API 地址配置和分包完成。缺容器、生产 WSGI、环境分层与部署清单。 |

## 本轮完成的关键优化

### 工业证据链

- `query_telemetry_summary` 返回日内窗口的样本数、覆盖与空档、信号统计、计数器变化、状态采样和原始遥测引用。
- `query_line_timeline` 合并一条产线的 CNC、Robot、PLC 事件，提供稳定事件引用、分页游标和准确的本页时间窗口。
- `query_line_kpi` 返回产线指标、加权公式、三个工位贡献、分钟/百分数单位及数据警告。
- 系统提示明确状态码语义、分钟单位、分页边界，以及事件结束时间不能自动解释为故障恢复。

### 编排、成本与可靠性

- 遥测、产线 KPI、产线时间线和纯知识问题使用高置信路由。参数完整时先执行固定工具，再让模型总结，避免模型反复探索无关工具。
- 模型只能调用当前问题暴露的工具；未知或臆造工具会被编排层拒绝。
- 网络错误、429 和可恢复 5xx 使用有限指数退避与随机抖动，记录每次 provider 请求和尝试次数。
- 对索取密钥、写 SQL、绕过安全联锁的请求在模型调用前拦截，相关评测消耗 0 token。
- 会话通过 `conversation_id` 保留最近 8 条消息，30 分钟过期并限制最多 1000 个会话。

### 安全、审计与数据

- Agent 请求体限制为 32 KB，CORS 默认只允许本机前端，可配置 Bearer token，默认并发上限为 4。
- 轮转 JSONL 审计只记录运行元数据，不记录用户问题、回答、工具参数或原始结果。
- 健康接口检查 MySQL、知识库 revision、模型配置、7 个工具和审计目录。
- 演示库增加 `raw_telemetry(equip_id, timestamp)`、`status_event_log(equip_id, start_time, end_time)` 和日报 `(date, equip_id)` 唯一索引。原 `yzl` 数据库保持不动，因为旧日报存在重复记录，需要迁移前清理。

### 工程交付

- 初始化 Git 并排除 `.env`、虚拟环境、依赖、构建产物、运行日志和本地数据。
- GitHub Actions 使用 MySQL 8.4，运行后端测试、离线检索评测、前端构建和受控密钥文件检查。
- ECharts 与 Element Plus 改为按需注册和独立 chunk。业务入口由旧版约 722.9 KB gzip 的单包降至 28.8 KB gzip；图表、UI 和 Markdown 可独立缓存，构建不再出现大包警告。

## 最终验证基线

- 后端：**44/44** 项 `unittest` 通过。
- RAG：**14/14** 案例通过；Hit@1 **0.9231**、Hit@3 **1.0**、MRR **0.9615**、无关问题拒绝率 **1.0**；revision `fee1141558dfe536`。
- DeepSeek：**8/8** 真实模型案例通过；总计 **16,781 tokens**，平均 **2,097.62 tokens/例**，累计端到端耗时约 **32.15 秒**。
- 前端：Vite 生产构建通过；业务入口 **76.23 KB / 28.81 KB gzip**，无大包警告。

## 距真正企业生产级还缺什么

1. **身份与数据权限**：接企业 IdP，采用短期 JWT、RBAC/ABAC、租户和设备范围隔离；建立独立数据库迁移账号与仅 `SELECT` 的运行账号。
2. **多实例运行**：将会话、限流和任务状态迁到 Redis/PostgreSQL；模型调用进入任务队列，增加总超时预算、熔断、provider fallback 和取消机制。
3. **可观测与 SLO**：接 OpenTelemetry、集中日志、指标看板和告警，定义成功率、P95 延迟、成本、引用覆盖率和工具错误率目标。
4. **知识治理**：取得厂商授权手册和现场 SOP，补齐负责人、适用型号、版本、失效策略与审批流；语料扩大后再引入 embedding、混合检索和 reranker。
5. **评测规模**：扩到 30–50+ 版本化案例，保存脱敏工具 fixture 和模型响应 replay，增加逐字段数值忠实度、注入攻击、长对话、并发和故障演练。
6. **部署与工业安全**：拆分 OPC UA 仿真、采集、API 和 worker，使用生产 WSGI/容器/环境密钥管理；连接真实设备前完成网络分区、只读点位和现场安全评审。

这些工作需要真实组织账号、授权资料或部署环境。当前项目已经适合在实习面试中讲清楚 Agent 的工具边界、取证流程、RAG 治理、评测和工程取舍；应准确称为“企业级原型”，不要宣称已在生产环境落地。
